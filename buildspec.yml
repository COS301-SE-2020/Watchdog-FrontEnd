version: 0.2

phases:
  install:
    commands:
      - npm install
  build:
    commands:
      - npm run build
      - npm test
cache:
  paths:
    - 'node_modules/**/*'
artifacts:
  files:
    - '**/*'
appRoot: watchdog-frontend/